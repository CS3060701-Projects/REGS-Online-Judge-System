# 期末專案評分檢查


## 1. 自動化編譯管道 (20 分)

| 評分項目 | 規格要求 | 程式碼實作位置 / 檢查點 |
| :--- | :--- | :--- |
| **ZIP 上傳與解壓縮** | 支援接收 .zip，解壓縮並檢查 `CMakeLists.txt`。 |  `internal/api/handlers/submission_prepare.go` 裡的 `ensureCMakeProject` 確保並複製 `CMakeLists.txt`。<br> `internal/api/handlers/admin.go` 的 `CreateProblem`：處理題目上傳並解壓縮。 |
| **容器網段隔離** | 執行階段使用 `--network none` 防止網路攻擊。 |  `internal/judge/sandbox.go` 第 264 行：執行測資 (Preset/CTest) 時的 docker 指令明確帶有 `--network none`。 |
| **CMake 管線自動化** | 自動執行 Configure 與 Build，抓取 Exit Code。 |  `internal/judge/sandbox.go` 裡的 `RunCompile`。透過 `cmd.Run()` 捕捉底層 Exit 狀態，並依此判斷是否為 CE/SE。 |
| **五種基本狀態判定** | AC、WA、CE、RE、TLE。Exit Code 非 0 視為 RE。 |  `internal/judge/sandbox.go` 的 `isRuntimeError` (第 652 行)：攔截 `Exception` 與非零的 `Exit code` 以判定 **RE**。<br> `compareOutput` 函式：比對檔案決定 **AC/WA**。<br> `context.WithTimeout` 配合 `DeadlineExceeded` 決定 **TLE**。 |
| **多分段日誌查詢** | 實體儲存為 config.log、compile.log、output.log。 |  `internal/judge/sandbox.go` 裡的 `mergePresetLogs` 將各 Preset 的輸出整合成 `config.log` 與 `compile.log`。|

## 2. 進階功能 (10 分)

| 評分項目 | 規格要求 | 程式碼實作位置 / 檢查點 |
| :--- | :--- | :--- |
| **非同步任務隊列** | 接收提交後立即回傳 `operatorId`，背景評測。 |  `internal/api/handlers/submission.go` 第 113 行：提交 API 立即 `c.JSON` 回傳 `operatorId`，並透過 `JobQueue <- JudgeJob{...}` 將任務送入背景佇列。 |
| **併發控制機制** | 限制最大容器數量，實作 Semaphore。 |  `internal/api/handlers/judger.go` 第 24 行：透過 `semaphore = make(chan struct{}, maxWorkers)` 實作 Semaphore 來限制最大併發數量。 |

## 3. 權限控管與查詢功能 (20 分)

系統共實作了 16 支 API，對齊規格表要求的 Admin、User、Guest 權限。

- **路由設定與權限綁定**：
   `cmd/server/main.go`：透過 Gin Group 設定 API 路由。
   `internal/api/middleware/auth.go`：實作 `JWTAuthMiddleware` 與 `RoleAuthMiddleware` 進行 RBAC 控管 (例如 `RoleAuthMiddleware("Admin")`)。
- **JWT 簽署與認證**：
   `pkg/jwt/jwt.go`：實作了基於 ECDSA P-256 的 JWT 簽署。
   `cmd/task/main.go`：自動執行 `ensureKeys()` 產生 `private.pem` 與 `public.pem`。

## 4. 文件 (10 分)

| 評分項目 | 規格要求 | 實作位置 / 檢查點 |
| :--- | :--- | :--- |
| **OpenAPI 3.0 API 文件** | 提供標準 API 文件。 |  `docs/openapi.yaml`：完整的 OpenAPI 3.0 規格文件。 |
| **ERD 實體關聯圖** | 資料庫結構與關聯。 |  `ERD實體關聯圖.md`：包含 Mermaid 關聯圖與 Role 定義。 |
| **專案操作說明** | 指導如何部署與執行。 |  `README.md`：包含跨平台 `cmd/task/main.go` 啟動教學與 ZIP 打包規範。 |

---
